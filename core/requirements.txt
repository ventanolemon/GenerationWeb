# Зависимости headless-ядра. После lazy-импорта PyQt6 в blocks.py и rendering.py
# Qt больше не нужен на уровне модуля — этот файл подходит для серверной
# установки (FastAPI, Docker, CI).
#
# PyQt6 нужен ТОЛЬКО для запуска десктоп-UI из старого репозитория. В вебе
# его установка не требуется.

python-docx>=1.0.0
matplotlib>=3.7.0
numpy>=1.24.0
sympy>=1.12
Pillow>=10.0.0
